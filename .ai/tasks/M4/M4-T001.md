# M4-T001 — Admin auth: argon2 seed, login A-01, sessions, lockout, cookies/CSRF

## Objective
ADMIN_PASSWORD_HASH bootstrap, login screen, admin_sessions (hashed tokens), lockout per username AND truncated IP (5/15min), sy_admin/sy_csrf contracts, rotation on login.

## Why This Task Exists
The only authentication in the product, scoped to /admin.

## Authoritative References
- SCREENS.md A-01
- API.md §5/5.1 login
- SECURITY.md §6
- DATABASE.md §2.13/2.14

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M3-GATE (phase gate)
- M1-T011

## Blocks
- M4-T002…T009

## Scope
Auth service + login route only.

## Required Implementation
No public auth of any kind (law 3); audit auth.login/auth.fail.

## Acceptance Criteria
- [ ] Lockout counters + no-enumeration behavior (T-32)
- [ ] Cookie/CSRF contracts match SECURITY §6 exactly
- [ ] Sessions 8h/24h enforced

## Tests
Auth integration tests.

## Verification
M4-GATE 4.4 evidence.

## Files / Areas Expected To Change
/app/admin/login,/lib/auth

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**
