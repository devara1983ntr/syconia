# M2-T004 — Normalization pipeline (NormalizedVideo DTO, field-wise Zod, quarantine)

## Objective
normalize(): field-wise validation, charset/length clamps, URL allowlist checks, unknown→metadata jsonb, drift logging per PRD2 §6.

## Why This Task Exists
Source data is untrusted; normalization is the trust boundary.

## Authoritative References
- PRD2.md §6
- API.md §6.3
- SECURITY.md §13
- DATABASE.md §2.2

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M2-T003

## Blocks
- M2-T005
- M2-T009

## Scope
Normalizer + DTO schemas only.

## Required Implementation
Field-wise drop semantics (never whole-batch); slug generation per DATABASE §2.2 algorithm.

## Acceptance Criteria
- [ ] Contract-accurate fixtures normalize correctly
- [ ] Bad fields drop + log drift; bad items count
- [ ] Slug algorithm + collision retry tested

## Tests
Unit tests with recorded fixtures (TESTING §1).

## Verification
Suite output.

## Files / Areas Expected To Change
/lib/adapters/normalize

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion)

## Status
**NOT_STARTED**
