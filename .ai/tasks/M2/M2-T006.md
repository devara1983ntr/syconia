# M2-T006 — Ops jobs: probe-availability, refresh-trending, rollup-daily, purge-expired, sitemap-refresh + cron wiring

## Objective
Implement remaining canonical-registry jobs (ARCH §5) incl. trending score math (PRD2 §3.1), views_24h/7d maintenance, 90d purge, retention 30–180 setting, cron endpoints with CRON_SECRET.

## Why This Task Exists
Rankings, retention, and sitemap freshness depend on these.

## Authoritative References
- ARCHITECTURE.md §5 jobs registry
- PRD2.md §3
- DATABASE.md §2.2/§6
- CI-CD.md §7

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M2-T005

## Blocks
- M2-T019
- M5-T002

## Scope
Jobs + cron endpoints only.

## Required Implementation
All idempotent + audited; purge honors tunable retention.

## Acceptance Criteria
- [ ] Trending math matches §3.1 (unit-tested weights)
- [ ] Purge deletes only expired raw rows
- [ ] Cron auth enforced

## Tests
Unit (math) + integration (purge) tests.

## Verification
M2-GATE 2.11 evidence.

## Files / Areas Expected To Change
/scripts/jobs,/app/api/cron

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion)

## Status
**NOT_STARTED**
