# M2-T001 — Drizzle schema + migrations for all 18 tables

## Objective
Implement DATABASE §2.1–2.18 exactly (columns, enums, FKs, CHECKs, uniques, indexes incl. GIN tsvector/trigram, generated search_vector) as drizzle migrations.

## Why This Task Exists
The catalog is the data plane for every feature.

## Authoritative References
- DATABASE.md all
- ARCHITECTURE.md §2

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T001
- M1-T007

## Blocks
- M2-T005
- M2-T010…T014
- M4-T009

## Scope
Schema + generated SQL migrations only (no business logic).

## Required Implementation
drizzle-kit generate; expand/contract policy for destructive changes; roles NOT included (T002).

## Acceptance Criteria
- [ ] Migration applies clean on empty + populated DB (G-4)
- [ ] pg_trgm/tsvector extensions idempotent
- [ ] EXPLAIN uses hot-path indexes (§5)

## Tests
Integration migrations test + EXPLAIN asserts (T-78/T-79).

## Verification
M2-GATE 2.1/2.2 evidence.

## Files / Areas Expected To Change
/drizzle,/lib/db

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion)

## Status
**NOT_STARTED**
