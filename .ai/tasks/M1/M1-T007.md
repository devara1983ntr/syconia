# M1-T007 — App config + secure defaults (endpoint config, no secrets)

**Migration disposition (v1.1.0):** MODIFY (was env.ts)

## Objective
Non-secret BuildConfig (API base URL per variant); debug/release separation; network-security-config (cleartext banned); no secrets in APK (verified); backend env handling stays server-side.

## Why This Task Exists
Fail-closed config; zero client secrets (SECURITY §2A).

## Authoritative References
- SECURITY.md §2A
- ARCHITECTURE §12 (backend, unchanged)

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); toolchain per M0-T003; AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T011
- M1-T019

## Scope
App-side config only.

## Required Implementation
Endpoint per build type (dev/staging/prod); APK secret scan in CI.

## Non-functional Notes
- Security: Secret scan of APK clean.
- Accessibility: n/a
- Performance: n/a

## Acceptance Criteria
- [ ] Release APK contains zero secrets (scan artifact)
- [ ] Cleartext traffic impossible (config test)

## Tests
CI security stage outputs.

## Verification
G-6 android evidence.

## Files / Areas Expected To Change
/android/app, /android/core/network

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**


---

## Web-platform predecessor — COMPLETE (superseded)

- **Predecessor title:** M1-T007 — Env validation + Zod boundary schemas (/lib/env.ts, /lib/validation)
- **Platform / status:** Web (pre-Android baseline) · **COMPLETE** — executed 2026-09-05 by the web-track agent session, all acceptance criteria PASS with evidence
- **Evidence commits** (preserved, reachable via the 2026-09-07 reconciliation merge): `feat f7233a8`
- **Objective (web):** Fail-closed env validator (ARCH §12 vars incl. AGE_LEAVE_URL) + shared Zod schema home per SOP §3.
- **Disposition:** Superseded by the Android v1.1.0 redefinition above (this file's active content); the completed web implementation is **retained in-tree** (`app/`, `components/`, `lib/`, `tests/`, `scripts/`, web configs) as the web-platform track slated for M5. Full completion evidence: `git show f7233a8:.ai/tasks/M1/M1-T007.md`.
