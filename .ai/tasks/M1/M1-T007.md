# M1-T007 — Env validation + Zod boundary schemas (/lib/env.ts, /lib/validation)

## Objective
Fail-closed env validator (ARCH §12 vars incl. AGE_LEAVE_URL) + shared Zod schema home per SOP §3.

## Why This Task Exists
Boot must fail on missing/weak secrets; single validation source for client+server.

## Authoritative References
- ARCHITECTURE.md §12
- SOP.md §3
- SECURITY.md §11
- .env.example

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T011
- M2-T010…T014
- all API tasks

## Scope
env.ts + validation module only.

## Required Implementation
Zod schemas for every ARCH §12 var (length ≥32 secrets, https URLs); process.exit on invalid.

## Acceptance Criteria
- [x] Boot fails closed on missing/weak env (test)
- [x] All §12 vars validated
- [x] Secrets never logged

## Tests
Unit tests with valid/invalid env fixtures.

## Verification
Fail-closed test output.

## Files / Areas Expected To Change
/lib/env.ts, /lib/validation

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent. Repository integrity at start: clean tree, HEAD `e273ab8` == `origin/main`.
- **Dependency:** `zod@4.5.4` (locked stack, ARCHITECTURE §2; runtime dependency; registry-verified M0-T003).
- **`lib/validation/index.ts` (shared schema home, SOP §3 "Zod at every boundary"):** reusable primitives every boundary schema will compose — `secretSchema` (≥32 chars, .env.example header rule) · `httpsUrlSchema` (https-only) · `postgresUrlSchema` (postgres/postgresql schemes) · `argon2idHashSchema` (encoded-format regex + SECURITY §11 parameter minimums m≥19456 KiB/t≥2/p≥1, issue text cites the rule) · `positiveIntSchema` (RATE_LIMIT_* family) · `SafeIssue`/`toSafeIssues` (ZodError → [{varName, reason}] — never values).
- **`lib/env.ts` (fail-closed validator, ARCH §12):** schema for every §12 variable — DATABASE_URL/DATABASE_POOL_URL (postgres URLs) · AGE_SECRET/CURSOR_SECRET/SESSION_SECRET/CRON_SECRET (secrets ≥32) · ADMIN_USERNAME (non-empty) · ADMIN_PASSWORD_HASH (argon2id + §11 minimums) · SITE_URL (https) · AGE_LEAVE_URL (https; SCREENS S-01 leave target) · SITE_NAME default "SYCONIA" · LOG_LEVEL enum default "info"; `passthrough()` + `superRefine` pattern-families: every `RATE_LIMIT_*` present must be a positive integer, every `SOURCE_<SLUG>_KEY` must be ≥32 (slug charset [A-Z0-9]+ segments). Exports: `parseEnv(source)` (pure, throws `EnvValidationError` whose message + `safeIssues` carry names+reasons ONLY) · `getEnv()` (cached process.env accessor for server code) · `bootValidateEnv({source, exit, log})` (logs the safe message, exits 1; non-terminating injected exit re-throws — fail-loud; only caches when source === process.env).
- **`instrumentation.ts` (root, Next boot hook):** `register()` runs `bootValidateEnv()` once per server boot on the Node runtime (NEXT_RUNTIME guard; edge skipped) — the ARCH §12 "validated at boot" wiring.
- **Local dev config:** `.env.local` created (git-ignored, verified via `git check-ignore`): valid-format values against the local rootless PG + 48-char random secrets; `RATE_LIMIT_READS_PER_MIN=120` exercises the pattern family.
- **Operator pitfall discovered & documented (real finding):** dotenv-expand interpolates `$word` inside .env values — an argon2id encoded hash (`$argon2id$v=19$…`) is MANGLED unless dollars are escaped (`\$`). Verified programmatically (raw → `"=19=19456,t=2,p=1"`, escaped → literal). `.env.local` escaped; guidance comment added to `.env.example` (same-commit doc fix, AGENT law 10 spirit). M4-T001 (hash-password tooling) must emit/instruct escaped output — recorded for that task.
- **Tests (42, all green):** `tests/unit/validation.test.ts` (15) — primitives incl. argon2 param boundaries (m=19455/19456, t=1/2, p=0/1), positive-int edges, toSafeIssues value-leak assertions. `tests/unit/env.test.ts` (23) — valid parse + defaults + passthrough · **it.each over all 10 required vars (missing → throw, message names the var)** · weak-secret rejection with no-value-leak assertion · http URLs rejected · mysql URL rejected · bad LOG_LEVEL · weak argon2 params · RATE_LIMIT_*/SOURCE_* family accept/reject · SafeIssue structure · **bootValidateEnv: exit(1) + log once + message excludes the weak secret value + re-throw on non-terminating exit; valid → returns env, exit NOT called** · getEnv caching (vi.stubEnv) · `.env.example` coverage parity (all 12 concrete §12 vars documented). `tests/unit/instrumentation.test.ts` (2) — register() calls bootValidateEnv on the nodejs runtime; skipped on edge.
- **Boot proofs (fresh servers, port 3100, `next start`):** (1) **WITHOUT .env.local → FAIL-CLOSED:** server prints "SYCONIA boot aborted — refusing to start with an invalid environment." + 10 safe issues (names+reasons, zero values) then exits — connection refused afterwards. The ambient platform `DATABASE_URL=file:…` (SQLite URL of the sandbox preview app) was correctly REJECTED as non-postgres — the gate also protects against environment pollution. (2) **WITH .env.local (+ inline valid DATABASE_URL) → HEALTHY:** Ready in 105ms, GET / 200, no abort. Caveat recorded: @next/env does not let .env values override pre-set process.env vars (dotenv no-override) — in this sandbox the platform DATABASE_URL must be overridden inline when booting SYCONIA; production (operator-supplied real env) has no such conflict.
- **Verification (all green):** `npm run test` → **102/102** (8 files) · typecheck clean · lint 0/0 · G-7 PASS · G-8 PASS · build PASS (3/3 static; instrumentation compiles, register does not run at build time).
- Commit: `feat(env): fail-closed Zod environment validation + shared schema home (M1-T007)` (this repository, 2026-09-05).

## Status
**COMPLETE**
