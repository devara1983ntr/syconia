# M1-T003 — Implement design tokens layer + Tailwind v4 wiring

## Objective
tokens.css with all DESIGN-SYSTEM §4–§6 tokens (colors incl. computed contrasts, spacing 8px scale, radii, elevations, focus, breakpoints §8) consumed via Tailwind @theme.

## Why This Task Exists
Tokens are the only color/spacing source (law 6, G-8); every primitive and screen inherits them.

## Authoritative References
- DESIGN-SYSTEM.md §4/§6/§8
- branding/design-tokens.json (token foundation staged 2026-09-03 — mirror it, do not fork) · branding/ASSET-MANIFEST.md
- ARCHITECTURE.md §2

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T008…M1-T010
- M1-T013
- all UI tasks

## Scope
Token layer + Tailwind wiring + fluid type clamp() scale only.

## Required Implementation
Exact values from DESIGN-SYSTEM tables; semantic + brand primitives; no extra colors.

## Acceptance Criteria
- [x] All §4 tokens present with computed ratios (18.9/9.9/5.6/8.1/13.4…)
- [x] No raw hex outside tokens file (lint green)
- [x] Fluid type scale per §5

## Tests
Token snapshot unit test vs spec table.

## Verification
G-8 green; token test green.

## Files / Areas Expected To Change
/app/styles/tokens.css, tailwind config

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent. Repository integrity at start: clean tree, HEAD `2c5158c` == `origin/main`.
- **Tailwind v4 wired (locked stack, ARCHITECTURE §2):** `tailwindcss@4.3.3` + `@tailwindcss/postcss@4.3.3` (exact versions; registry-verified in M0-T003) installed via lockfile-only + `npm ci`; `postcss.config.mjs` registers the plugin. No `tailwind.config.js` — v4 is CSS-first; the token file IS the theme.
- **Token layer `/app/styles/tokens.css` (the ONLY raw-hex file, mirroring branding/design-tokens.json v1.0.0 verbatim — no fork):** `@theme` block with §4 brand primitives (5) + semantic tokens (16) · §5 fluid type scale (8 steps + line-height/letter-spacing/weight modifiers, clamp values verbatim) + licensed family stacks (Fraunces/Inter named; self-hosting arrives M1-T004) · §6 spacing ladder (8px base + named 1–8 + section rhythm 64/96/128) · radii sm 6/md 10/lg 16/full · elevations 1/2/3 (shadows only) · glass blur 12px · §8 breakpoints 480/768/1024/1280/1440 · D-006 container content 1280px. **Default-theme pruning:** `--color-*/--font-*/--text-*/--radius-*/--shadow-*/--blur-*/--breakpoint-*/--container-*: initial` removes every non-spec default so ONLY spec-exact values exist (law 6 hardening). Non-utility tokens as plain `:root` custom properties: D-006 z-index ladder (8 steps), focus construction (2px ring + 2px offset), hairline weight, grid gutters 16/24, touch targets 44/48, measure 70–80ch.
- **`app/styles/globals.css`:** `@import "tailwindcss"` + tokens + base layer applying tokens via CSS variables ONLY (zero raw hex — G-8 clean): color-scheme dark, body background/text/font/size from tokens, heading text-wrap balance (§5), `:focus-visible` champagne 2px+2px construction (§4/ACCESSIBILITY §2), selection colors, §9 reduced-motion reset. Wired into `app/layout.tsx`.
- **Token snapshot unit test (`tests/unit/tokens.test.ts`, 17 tests):** parses tokens.css declarations and asserts the spec table verbatim (colors, type scale incl. clamp strings, spacing, radii, elevations, blur, breakpoints, container, z-ladder, focus/targets/measure) **+ WCAG contrast re-verification per DESIGN-SYSTEM §4 (normative): all 8 text/status ratios RE-COMPUTED against Obsidian #09090B** — 18.9/9.9/5.6/8.1/13.4/9.7/10.2/8.2 all match, all pass AA ≥4.5:1, focus ring ≥3:1.
- **Spec correction (AGENT law 10 — same-commit doc fix):** `--color-success #4EC9A0` computes **9.6504:1 → 9.7:1**; the spec tables (DESIGN-SYSTEM §4 + branding/design-tokens.json) stated 9.6:1 — a truncation error. Corrected in BOTH files 9.6 → 9.7 with this commit (computed math governs conformance per §4's own B-1 rule; the hex value itself was and remains correct).
- **G-8 gate extended (CSS pass):** `scripts/ci/lint-rules-check.sh` now also greps all `*.css` under production paths for raw hex, exempting exactly `app/styles/tokens.css` — closes the CSS blind spot of the ESLint (JS/TS-only) rule.
- **Verification (all green):** `npm run test` → **17/17 passed** (2 files) · `npm run lint` 0/0 · `npm run ci:lint-rules` PASS (3 rules fire on fixtures + no raw hex outside token layer) · `npm run ci:no-placeholder-gate` PASS (G-7) · `npm run typecheck` clean · `npm run build` PASS (`/` + `/_not-found`, 3/3 static). **Dev-boot smoke:** `next dev :3100` → HTTP 200; served stylesheet `app_styles_globals_*.css` verified to contain the token emissions (`--color-brand-gold: #C5A059`, `--text-display: clamp(34px, 6vw, 64px)`, scrim/glass/radius-full/blur-glass/z-ladder/target vars, reduced-motion block) — Tailwind v4 emits `@theme` vars as `:root` custom properties and JIT-generates utilities on first use (none yet — home route is intentionally stripped; primitives arrive M1-T008+).
- Commit: `feat(design): implement token layer + Tailwind v4 wiring (M1-T003)` (this repository, 2026-09-05).

## Status
**COMPLETE**
