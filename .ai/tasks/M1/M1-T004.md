# M1-T004 — Self-host fonts (Fraunces + Inter) via next/font

## Objective
Load Fraunces (serif) + Inter (sans), subsets, preload, font-display swap per PERFORMANCE §2.

## Why This Task Exists
Legal OFL fonts, zero layout shift, brand typography direction.

## Authoritative References
- DESIGN-SYSTEM.md §5
- branding/fonts/ (Fraunces + Inter VF staged 2026-09-03, SIL OFL 1.1, SHA-256 in branding/ASSET-MANIFEST.md — consume these files; no downloads/substitutions)
- branding/README.md §Typography guardrail AG-013: Fraunces VF default instance is wght 900 — pin next/font weight range + CSS font-weight (≥400) explicitly; never rely on VF defaults
- ARCHITECTURE.md §2
- PERFORMANCE.md §2

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T003

## Blocks
- M1-T008…M1-T010
- all UI tasks

## Scope
Font loading + type utility classes only.

## Required Implementation
next/font with OFL packages; weights limited to spec (2 families × 2 weights initial).

## Acceptance Criteria
- [x] Fonts self-hosted (no external requests)
- [x] LCP/CLS unaffected (swap+preload)
- [x] Typography classes match §5 scale

## Tests
Build + network-request assertion test.

## Verification
No external font requests in test output.

## Files / Areas Expected To Change
/app/styles, layout

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent. Repository integrity at start: clean tree, HEAD `b581ac1` == `origin/main`.
- **`/app/fonts.ts` (next/font/local, the ONLY loading seam):** both faces consume the officially staged OFL packages straight from `branding/fonts/` — no downloads/substitutions. File integrity pinned by test: `sha256(Fraunces-VF.ttf) = 177ff6c0…58abb` and `sha256(Inter-VF.ttf) = 29160a80…dc559031` (ASSET-MANIFEST ASSET-FONT-001/ASSET-FONT-002, exact match). One variable file per family covers the whole weight range — first-load budget = exactly 2 font files (PERFORMANCE §2).
- **AG-013 (branding/README):** Fraunces VF default instance is wght 900 — both faces declare `weight: "400 900"`, so the emitted `@font-face` carries `font-weight: 400 900` and no text can render below 400 (requests <400 clamp to the range minimum per CSS font matching; DESIGN-SYSTEM §5 "no font weights below 400"). `style: "normal"` (D-008: no italic; both VF files are upright-only). CSS-side re-verified: every `--text-*--font-weight` token ≥ 400 (test-enforced).
- **PERFORMANCE §2:** `display: "swap"` · `preload: true` (Next emits `<link rel="preload" as="font" crossorigin>` for both faces) · `adjustFontFallback` metric faces emitted (`fraunces Fallback` ← Times New Roman 76.84%/127.27%, `inter Fallback` ← Arial 89.79%/107.89% ascent/size-adjust) — swap-driven layout shift metric-compensated.
- **Wiring:** `app/layout.tsx` applies `${fraunces.variable} ${inter.variable}` to `<html>` (`--font-fraunces`/`--font-inter` hashed-family variables). `app/styles/tokens.css` §5 families now resolve through them: `--font-sans: var(--font-inter, "Inter"), ui-sans-serif, …` / `--font-serif: var(--font-fraunces, "Fraunces"), ui-serif, …` (literal names kept only as inert non-layout/test fallbacks). Type utilities unchanged (§5 scale snapshot still 17/17).
- **Tests (`tests/unit/fonts.test.tsx`, 14 tests + 3 post-build):** module contract via `vi.hoisted` capture of `next/font/local` calls (src, weight pin, style, swap, preload, adjustFontFallback, variable names) · SHA-256 pins vs ASSET-MANIFEST · layout wiring (root layout element injects both font-variable classes on `<html>`, `lang="en"`, `<body>` wraps children) · tokens.css var-resolution regexes · type-weights ≥ 400 · **post-build self-hosting proof (runs with build output present):** prerendered `index.html` contains ≥2 `as="font"` preloads pointing at `/_next/static/media/*.ttf` and ZERO external font URLs; built CSS chunks contain exactly 2 primary `@font-face` (swap, `400 900`, normal) whose `src` files exist on disk + 2 metric `Fallback` faces.
- **Lint debt fix (pre-existing, found by this round's battery):** at `b581ac1` baseline, `npm run lint` reported 20 `syconia/no-raw-hex` errors from `tests/unit/tokens.test.ts` (M1-T003's spec-snapshot table — the verification source, not production styling). Fixed via the sanctioned G-8 exemption path: `allowedFiles: ["app/styles/tokens.css", "tests/unit/tokens.test.ts"]` with the justification documented in `eslint.config.mjs`. Verified at baseline via `git stash` → errors present → `git stash pop` → exemption → clean. (M1-T003's recorded "lint 0/0" did not hold for its final state — corrected here, AGENT truthfulness law.)
- **Verification (all green):** `npm run test` → **34/34 passed** (3 files) · `npm run typecheck` clean · `npm run lint` 0/0 · `npm run ci:no-placeholder-gate` PASS (G-7) · `npm run ci:lint-rules` PASS (G-8: 3 rules fire on fixtures, no raw hex outside the token layer) · `npm run build` PASS (`/` + `/_not-found`, 3/3 static). **Prod-boot smoke (`next start :3100`):** HTML head preloads both `.ttf` files; `@font-face` served with `font-display:swap`, `font-weight:400 900`, normal style; direct font fetch HTTP 200 with `360440` bytes (exact `Fraunces-VF.ttf` size); `--font-sans/--font-serif` var chains present in served CSS; `0` matches for `fonts.googleapis|fonts.gstatic`.
- Commit: `feat(fonts): self-host Fraunces + Inter via next/font with AG-013 weight pinning (M1-T004)` (this repository, 2026-09-05).

## Status
**COMPLETE**
