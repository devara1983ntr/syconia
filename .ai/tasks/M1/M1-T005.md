# M1-T005 — Integrate official brand assets (favicon, icons, logo components)

## Objective
Favicon 32, app icons 192/512, manifest, symbol/wordmark components with clear-space token --space-logo-clear; watermark export (128px @20%).

## Why This Task Exists
Official /branding/ assets only — logo recreation is forbidden (law 6; DS §2–3).

## Authoritative References
- DESIGN-SYSTEM.md §2/§3/§7
- branding/ assets + branding/ASSET-MANIFEST.md (full pack staged 2026-09-03: favicon 16/32/ico, app icons 192/512/180, transparent + mono variants — this task wires them; do not re-derive) · branding/README.md (usage rules + favicon configuration)
- branding/app-icon/syconia-app-icon-maskable-512/-192.png (staged 2026-09-03 — manifest icons need `purpose: any` + `maskable` entries; SEO.md §Favicons/app-icons; derivation D-009)

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T003
- M1-T004

## Blocks
- M1-T013
- M3-T001 (watermark)

## Scope
Asset copying/optimization + React logo components + metadata icons.

## Required Implementation
Copy exact assets; components enforce min sizes (wordmark ≥96px, symbol fallback); clear-space rule.

## Acceptance Criteria
- [x] Favicon/app icons served from official files
- [x] Logo components render primary/symbol/mono variants
- [x] Clear-space token applied
- [x] No recreated SVG paths

## Tests
Visual snapshot tests of variants.

## Verification
Snapshot diff clean.

## Files / Areas Expected To Change
/app/favicon, /branding refs, components/brand

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent. Repository integrity at start: clean tree, HEAD `b016330` == `origin/main`. Battery at start: 102/102, all gates green (regression confirmed before starting).
- **Ostiole measurement (decision D-010, recorded in .ai/DECISIONS.md):** the guidelines' "central ostiole dot" = the emblem's central neck opening (the syconium's ostiole — botanically the enclosed bloom's opening, on the central axis; the official 32px thickened favicon renders this junction as the compact dot the guidelines call "fully legible at 16×16"). Measured programmatically on the official masters: **105 px** opening on the **615 px**-wide symbol artwork (primary master's emblem: 108 px on its ~590 px emblem — the same drawing). Clear space = 4 × 105 = 420 master-px = **0.6829 of the emblem width**. Pixel-scan evidence: narrowest 2-run gap search over every row of `syconia-symbol-only.png` (walls (731,764)/(870,903) at y=531 → gap 105 px; no other candidate feature — full connected-component analysis returns a single component of 98,980 px, bbox (510,219)-(1124,1371); the 7 small round components at y≈1405-1455 in `syconia-monochrome-light.png` are wordmark separator dots, not the emblem dot).
- **Token `--space-logo-clear: 0.6829268292682927`** added to `app/styles/tokens.css` :root (the spec-token layer, AGENT law 6 / G-8 territory) with the D-010 derivation comment; emitted verbatim in built CSS (verified: `--space-logo-clear:.682927` present in `.next/static/chunks/*.css`). Unit-enforced equal to `components/brand/geometry.ts` CLEAR_SPACE_RATIO (single source).
- **`components/brand/geometry.ts`** — measured master geometry (symbol/primary/mono-lockup/mono-symbol opaque bboxes + image dims, all pinned to ASSET-MANIFEST rows) + pure math: `imageBoxFor` (artwork width → `<img>` box, aspect-exact), `clearSpaceFor` (per-side shortfall vs the official masters' built-in transparent margins), `lockupFallbackSymbolWidth` (height-preserving symbol fallback).
- **`components/brand/logo.tsx`** — server components (zero client JS; PERFORMANCE §3 untouched): `Logo` (ASSET-LOGO-010 primary lockup; **enforces the §3 96px artwork floor** — below it renders `LogoSymbol` at the lockup's height, marked `data-fallback="logo-symbol"`), `LogoSymbol` (ASSET-LOGO-011), `LogoMono` (ASSET-LOGO-012 editorial lockup) and `LogoMonoSymbol` (ASSET-LOGO-013 empty/error/404 line-art) — mono variants decorative per branding/README a11y row (`aria-hidden`, empty alt). Every wrapper's padding is `max(0px, calc(var(--space-logo-clear) × emblem-width − master-margin))` per side — the token genuinely drives every placement, consumer styles can never override the §3 paddings, and the zone completes to exactly 4 dot-diameters on all four sides (never doubled: the official transparent masters' own margins are credited).
- **`components/brand/watermark.tsx`** — ASSET-WM-001 wiring per branding/README §Watermark verbatim: official 128px file at spec-fixed 128px display size, opacity .2, absolute bottom-right, 16px stage inset, `aria-hidden`, `pointer-events: none` (non-interactive, PRD §214), zIndex left to the M3-T001 stage composition (D-006 ladder). The 128px/20% export itself was already staged (ASSET-WM-001, DERIVED-VERIFIED) — this task wires it, no re-derivation (per task directive "do not re-derive").
- **Favicon/app icons/manifest wiring:** `app/layout.tsx` metadata.icons = branding/README §Favicon configuration **exactly** (`/favicon.ico` sizes any + official 32/16 PNGs + apple 180 from the app-icon master); `app/manifest.ts` (Next MetadataRoute, auto-linked at `/manifest.webmanifest`) carries `any` 192 (ASSET-ICON-002) + `any` 512 (ASSET-ICON-001 root master) + `maskable` 192/512 (ASSET-ICON-005/004 — the D-009 safe-zone-inscribed set), name/short_name "SYCONIA" (DS §1/PRD §36), truthful PRD project-line description (decoy strategy rejected, conflict C-2), `start_url:/`, `display:standalone`, `lang:en`, theme/background colors = §4 Obsidian (byte-equal to the token, unit-asserted; the manifest spec requires literal color strings — the reviewed G-8 exemption for `app/manifest.ts` is documented in `eslint.config.mjs`). **Built-output proof:** prerendered head contains exactly 1 manifest link + 3 icon links + 1 apple-touch link (no duplicates); `/manifest.webmanifest` body matches config 1:1; HTTP 200 on `/favicon.ico`, `/manifest.webmanifest`, and every referenced PNG (prod-boot smoke on :3100).
- **Static serve map (`public/branding/…` mirrors the pack 1:1 + `public/favicon.ico`):** 13 files, every one **byte-identical (SHA-256) to its `branding/` source** (test-pinned; acceptance "served from official files" + "no recreated SVG paths" — a recreation cannot match the registered hashes). PNG dims re-parsed from IHDR and asserted against ASSET-MANIFEST rows. The served set is exactly the map — nothing unregistered ships (recursive walk equality test). Serve map documented in `branding/ASSET-MANIFEST.md` §Static serve map (M1-T005).
- **Tests (43 new, 145 total):** `tests/unit/brand-geometry.test.ts` (15: ratio = 4×105/615 exactly; token==geometry single-source; master bboxes pinned to ASSET-MANIFEST; zone-completion property at 7 sizes × 4 masters — margin+padding = 4-dot zone per side, exact or already-satisfied; L/R auto-satisfied by symbol masters' margins; full-bleed mono gets the full ratio per side; fallback preserves lockup height; 96px floor) · `tests/unit/brand-assets.test.ts` (8: SHA-256 serve-map identity; ICO magic; IHDR dims vs register; exact-set equality; every component/manifest/metadata URL resolves) · `tests/unit/brand-components.test.tsx` (13: official src + box math + alt/aria contracts per variant; calc strings cross-checked against geometry math; 96px floor fallback + boundary at exactly 96; consumer-style override resistance; watermark spec constants; plain-`<img>` CLS-safe rendering with intrinsic width/height) · `tests/unit/manifest-metadata.test.ts` (7: README-exact icon config; D-009 any+maskable 192/512 set; locked name; truthful description; PWA fields; colors byte-equal the §4 token parsed live from tokens.css) · `tests/e2e/brand-visual.spec.ts` (**6 visual snapshot tests** — chromium baseline rasters of every variant served by the production server on the Obsidian token canvas at spec-relevant sizes: primary lockup 329×327, symbol 74 (sidebar) + 341 (age-gate band), mono symbol, mono lockup, favicon-32 + app-icon-192 + maskable-512, watermark 128 @ .2 opacity; **Verification row: snapshot diff clean — re-run without --update-snapshots passes 6/6**; token-sourced canvas color (no raw hex in test sources, G-8); chromium-only with the cross-engine rationale documented; firefox/webkit skip cleanly). Pixel-level baseline sanity: artwork coverage matches expected geometry (e.g. watermark 0.18% blended-gold pixels ≈ 50×92 art × 14% line coverage × 20% opacity).
- **Boot/env note (documented M1-T007 pitfall, reproduced + handled):** starting the prod server with the ambient platform `DATABASE_URL=file:…` in scope fails closed as designed ("SYCONIA boot aborted — DATABASE_URL must be a postgres:// URL", exit) — the :3100 smoke server runs with the documented inline `DATABASE_URL=postgresql://127.0.0.1:5432/syconia` override.
- **Verification (all green):** `npm run test` → **145/145** (12 files) · `npm run typecheck` clean · `npm run lint` 0/0 · `npm run ci:lint-rules` PASS (G-8; fixtures fire, no raw hex outside the token layer — the two mid-round raw-hex lint hits in test sources were fixed by sourcing colors from tokens.css, not by exemption) · `npm run ci:no-placeholder-gate` PASS (G-7) · `npm run build` PASS (`/`, `/_not-found`, `/manifest.webmanifest` — 3/3 static) · prod-boot smoke on :3100 (routes 200; head links exact; manifest body exact) · Playwright visual suite 6/6, snapshot diff clean.
- Commit: `2915f05` — `feat(brand): integrate official assets — favicon/manifest wiring + logo/watermark components (M1-T005)` (pushed to origin/main 2026-09-05; worktree clean after push).

## Status
**COMPLETE**
