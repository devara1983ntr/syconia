# M1-T009 — Primitives batch 2 — overlays & navigation (Badge, Tooltip, Dropdown, Modal, Drawer, BottomSheet, Tabs, Toast, Alert, Pagination, Breadcrumb)

## Objective
Implement overlay/navigation primitives with focus trap/restore, ESC, scrim, BottomSheet ≤767px.

## Why This Task Exists
Chrome (S-00), player failure overlays (E-07) and admin UI depend on these.

## Authoritative References
- DESIGN-SYSTEM.md §10
- SCREENS.md S-00
- ACCESSIBILITY.md §2

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T008

## Blocks
- M1-T013
- M3-T003
- M4 screens

## Scope
Eleven primitives + stories only.

## Required Implementation
Dialog semantics (role/aria-modal), focus return, toast region role=status, pagination Load-more + numbered.

## Acceptance Criteria
- [x] Focus trap + return verified
- [x] ESC closes topmost
- [x] Stories + axe clean

## Tests
RTL keyboard suites per primitive.

## Verification
Suite green.

## Files / Areas Expected To Change
/components/ui

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- **Executed 2026-09-05.**
- **Components (11/11 of the §10 batch-2 list):** `components/ui/overlay.tsx` (the shared core — module-level ESC-topmost layer stack per GESTURES §62, focus trap with wrap on Tab/Shift+Tab, focus return to the pre-open element, reference-counted body scroll lock, SSR-safe body Portal, §9 scrim variant on the --color-scrim token, D-011 popover surface register) + Modal / Drawer / BottomSheet (dialog role + aria-modal, labelled, scrim tap + built-in X + ESC, AnimatePresence exits, §9 modal/drawer/sheet variants via selectVariants incl. reduced-motion) + Tooltip (300ms delay, aria-describedby wiring, ESC-dismiss, wrapper-level focus/hover detection) + Dropdown (APG menu-button model: ArrowDown/ArrowUp open + focus, arrows cycle with wrap, Home/End, ESC + trigger focus restore, outside-pointerdown dismissal, Tab-continuation computed before close) + Tabs (ARIA tablist/tab/tabpanel, roving tabIndex, ArrowLeft/Right + Home/End with automatic activation, controlled + uncontrolled) + Toast (ToastProvider with role=status region per S-00/ACCESSIBILITY §3, §9 slide-up fade 200ms, 4s auto-dismiss with hover AND focus pause using remaining-time math, §7 variant icons, dismiss X) + Alert (role=alert assertive for warning/error, role=status polite for info/success, §7 icons, §4 tone borders) + Badge (outline + solid pills, §4 tones, 13px-legal contrast pairings) + Pagination (numbered with windowed ellipsis + aria-current + chevron IconButtons AND Load-more with the §3 live-region announcement line) + Breadcrumb (nav>ol>li, aria-current, §7 chevron separators, wraps at 320px).
- **Register:** `components/ui/states.ts` extended with the D-011 batch-2 values (DRAWER_WIDTH min(88vw,320px) from S-00 · MODAL_WIDTH min(92vw,480px) · SHEET_MAX_HEIGHT 90dvh · TOOLTIP_DELAY_MS 300 from §10 · MENU_MAX_HEIGHT 352 = 8×44px §7 floor) — unit-pinned in tests/unit/ui-overlay.test.tsx.
- **D-011 recorded in .ai/DECISIONS.md:** (1) SCREENS "scrim 40%" vs DESIGN-SYSTEM §4 --color-scrim 0.56 → the token layer wins ("the only color source"); (2) the D-006 z-ladder has no popover rung → anchored transients (tooltip, dropdown panel) map to --z-header-sticky (above base, below scrim); (3) modal/sheet/dropdown open layout values fixed in the states.ts register.
- **BottomSheet ≤767px law:** renders only while (max-width: 767px) matches (useSyncExternalStore on matchMedia — SSR snapshot false); growing past 767px while open calls onDismiss honestly (never renders at desktop); stories document the narrow-viewport requirement with an honest status line.
- **Spec-conflict fix found by the suites:** aria-label + aria-labelledby together made the visible title the accname (labelledby precedence) — components now set one or the other (title → labelledby; no title → the required label prop).
- **Tests: 79 new (275/275 total).** RTL keyboard suites per primitive incl. the acceptance matrix: focus trap wrap tests (Tab first↔last, Shift+Tab), focus-return-to-trigger, ESC-topmost with two stacked dialogs (first ESC closes only the top), body scroll lock on/off, scrim tap + X close, drawer S-00 geometry (88vw/320px, glass + 12px blur, --z-drawer), sheet mobile-only + honest auto-dismiss on viewport growth, tooltip 300ms show + leave-cancel + ESC, dropdown full APG keyboard model + Tab continuation, tabs arrows/Home/End + controlled/uncontrolled, toast 4s window + remaining-time pause math + region role + stacking, alert/badge/pagination/breadcrumb semantics, axe-core zero-critical per state matrix, pageWindow unit math. Pinned findings: userEvent's async act machinery deadlocks under vi fake timers in this React 19/jsdom env (fake-timer suites use fireEvent + act — documented); jest-dom pinned as an explicit devDependency (6.9.1, already resolved transitively) + tests/vitest-setup.ts wired via vitest setupFiles.
- **Storybook: 40 new stories (92 total in the build),** gallery build green (Vite 2.87s). Preview decorators now mount the REAL app contexts (MotionProvider + ToastProvider) so overlay/toast stories behave exactly as in the product. Keyboard play functions (dropdown/modal/drawer/sheet/tabs/pagination/toast-pause) via storybook/test.
- **Battery:** `npm run build` green (all routes 200 via `npm start` on :3100 with the inline DATABASE_URL override; server killed after proof); `npx tsc --noEmit` clean; `npm run lint` 0/0 (storybook-static/** added to eslint globalIgnores — generated build output, same class as .next; also .gitignored); `npm run ci:no-placeholder-gate` PASS; `npm run ci:lint-rules` G-8 PASS; `npm test` 275/275.
- **Commit:** feat(ui): primitives batch 2 — overlays & navigation with overlay core + Storybook gallery (M1-T009)

## Status
**COMPLETE**
