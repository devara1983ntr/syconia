# M1-T002 — Android quality toolchain (detekt, ktlint, unit/emulator harness, CI android track)

**Migration disposition (v1.1.0):** MODIFY (was web tooling)

## Objective
detekt+ktlint with custom rules (framework-import bans, tokens-only colors), JUnit/Turbine/MockK, Compose UI test harness, emulator matrix in CI, verifyNoPlaceholders gradle task (G-7), APK-size + startup budget gates.

## Why This Task Exists
Gates G-1/G-2/G-7/G-9 require the harness to exist first.

## Authoritative References
- TESTING.md §2 v1.1.0
- CI-CD.md §3 android track

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); toolchain per M0-T003; AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T005+
- all feature tasks

## Scope
Toolchain + one smoke test proving it runs; CI workflow files.

## Required Implementation
Custom lint conventions per ARCHITECTURE §2 prohibitions; emulator API 26+current in CI; deliberate-violation test proves G-7 blocks.

## Non-functional Notes
- Security: CI secret scan includes APK contents.
- Accessibility: n/a
- Performance: CI wall-time budget recorded.

## Acceptance Criteria
- [ ] `./gradlew testDebugUnitTest` smoke green
- [ ] detekt/ktlint enforce bans (verified with deliberate violations)
- [ ] verifyNoPlaceholders blocks a planted marker

## Tests
Smoke tests + violation fixtures.

## Verification
Toolchain outputs.

## Files / Areas Expected To Change
/android build-logic, CI files

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**


---

## Web-platform predecessor — COMPLETE (superseded)

- **Predecessor title:** M1-T002 — Install test/quality toolchain (Vitest, Testing Library, Playwright, axe, custom lint rules)
- **Platform / status:** Web (pre-Android baseline) · **COMPLETE** — executed 2026-09-05 by the web-track agent session, all acceptance criteria PASS with evidence
- **Evidence commits** (preserved, reachable via the 2026-09-07 reconciliation merge): `feat 2c5158c`
- **Objective (web):** Wire Vitest + RTL + Playwright + axe-core; add custom ESLint rules: no raw hex outside tokens, no dangerouslySetInnerHTML, no `any` (G-8).
- **Disposition:** Superseded by the Android v1.1.0 redefinition above (this file's active content); the completed web implementation is **retained in-tree** (`app/`, `components/`, `lib/`, `tests/`, `scripts/`, web configs) as the web-platform track slated for M5. Full completion evidence: `git show 2c5158c:.ai/tasks/M1/M1-T002.md`.
