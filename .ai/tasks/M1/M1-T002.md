# M1-T002 — Install test/quality toolchain (Vitest, Testing Library, Playwright, axe, custom lint rules)

## Objective
Wire Vitest + RTL + Playwright + axe-core; add custom ESLint rules: no raw hex outside tokens, no dangerouslySetInnerHTML, no `any` (G-8).

## Why This Task Exists
Gates G-1/G-3/G-8 and every task's Tests section require the harness to exist first.

## Authoritative References
- AGENT.md §6
- TESTING.md §2
- CI-CD.md §4 G-1/G-3/G-8

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T017
- all feature tasks

## Scope
Test config + CI-ready scripts only; one smoke unit test proving the harness runs.

## Required Implementation
Configure browsers matrix per TESTING §3; add lint-rule implementations; zero fabricated test cases.

## Acceptance Criteria
- [x] `npm run test` runs the smoke test green
- [x] `npm run lint` enforces the three custom rules (verified with deliberate violations)
- [x] Playwright launches Chromium/Firefox/WebKit

## Tests
Smoke unit test; lint violation fixtures.

## Verification
Toolchain outputs in evidence.

## Files / Areas Expected To Change
vitest/playwright configs, eslint rules, package.json

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent (owner approval for M1 recorded in M1-T001). Repository integrity at start: clean tree, HEAD `f206a39` == `origin/main`.
- **Toolchain installed** (lockfile regenerated `--package-lock-only`, installed via `npm ci`): `vitest 5.0.0` · `jsdom 30.0.1` · `@vitejs/plugin-react 6.1.1` · `@testing-library/react 16.3.3` (+ peer `@testing-library/dom 10.4.1`) · `@playwright/test 1.63.0` · `axe-core 4.13.0` · `typescript-eslint 8.69.0`. All match the M0-T003 registry-verified locked stack (vitest 5.0.0, @playwright/test 1.63.0, axe-core 4.13.0) and AGENT §3. `axe-core` is installed as the a11y engine — the Playwright binding lands with M1-T017 ("E2E smoke suite + axe wiring") per the task split; no scanner usage is fabricated before real tests exist.
- **Vitest wiring:** `vitest.config.ts` (jsdom environment, `@vitejs/plugin-react`, `@/` alias mirroring tsconfig paths, include `tests/unit/**` + `tests/component/**`) and `vitest.integration.config.ts` (node environment, `tests/integration/**` — CI-ready; runs fail loudly on zero test files rather than reporting green; first integration tests arrive with M2-T001). Script set: `test` = `vitest run`, `test:integration` = `vitest run -c vitest.integration.config.ts`, `test:e2e` = `playwright test` (real commands; the M1-T001 fail-closed guards for these three were replaced by the real toolchains — guards remain only for `db:*`/`hash-password`/`perf:lighthouse`).
- **Smoke unit test (real, zero fabricated cases):** `tests/unit/home-route.test.tsx` — RTL-renders the actual stripped scaffold home route and asserts the semantic landmark via `getByRole("main", { name: "SYCONIA home" })`. Output: `Test Files 1 passed (1) · Tests 1 passed (1)` (98ms).
- **G-8 custom lint rules:** `eslint-local-rules.mjs` registers the `syconia` plugin: `syconia/no-raw-hex` (word-boundary hex scan of string/template literals; `allowedFiles` option — default exemption `app/styles/tokens.css` per DESIGN-SYSTEM §13/M1-T003) and `syconia/no-dangerously-set-inner-html` (JSXAttribute check); plus `@typescript-eslint/no-explicit-any` via the real typescript-eslint plugin. All wired into `eslint.config.mjs` (plugin namespace + rules, error severity).
- **Deliberate violation fixtures (committed, regression-safe):** `tests/lint-fixtures/{raw-hex,no-danger,no-any}.fixture.{tsx,ts}` — each carries exactly one deliberate violation; the main config ignores the directory (G-1 stays clean) while `eslint.fixtures.config.mjs` + `scripts/ci/lint-rules-check.sh` (npm script `ci:lint-rules`) lint them with exactly the three rules and PASS only when all three fire: `G-8 lint-rule check: PASS — all three custom rules fired on the deliberate fixtures.`
- **Playwright wiring (TESTING §3):** `playwright.config.ts` — engine projects `chromium` / `firefox` / `webkit` (the Playwright-manageable trio; Chrome latest−1/Edge/iOS/Android/BrowserStack layers join per TESTING §3/§9 with the E2E suite M1-T017 and release matrix), `VIEWPORTS` export [320, 375, 390, 430, 768, 1024, 1280, 1440, 1920], `testDir tests/e2e`, webServer (`npm run dev`, `reuseExistingServer` locally, `SYCONIA_E2E_BASE_URL` override), retries 2 in CI, trace on-first-retry.
- **Browser launch acceptance — all three engines verified with launch + render + click:** `CHROMIUM … engine version 153.0.8010.12` · `FIREFOX … 155.0` · `WEBKIT … 26.6` (browsers installed via `npx playwright install chromium firefox webkit` → chromium-1243, firefox-1543, webkit-2359).
- **Environment provisioning note (this sandbox only; no repository changes):** the execution environment is rootless, so WebKit's GTK/GStreamer/EGL system dependencies were provisioned the documented M0-T003 way — official Debian packages (`libgtk-4-1, libgraphene-1.0-0, libgstreamer-gl1.0-0, libgstreamer-plugins-bad1.0-0, libsoup-3.0-0, libmanette-0.2-0, libenchant-2-2, libsecret-1-0, libgles2, libegl1, libgudev-1.0-0, libbacktrace0, libhidapi-hidraw0, libevdev2, libxkbcommon-x11-0, libnice10, libxcb-xkb1, libegl-mesa0`) fetched with `apt-get download` and extracted via `dpkg -x`. Libs live in the browser bundle's own supplemental-lib mechanism (`webkit-2359/minibrowser-wpe/sys/lib` — the wrapper script already prepends `sys/lib` to its LD_LIBRARY_PATH) plus an EGL GLVND vendor config at `~/.local/share/egl-vendors/50_mesa.json`. Playwright's pre-flight host validator checks dlopen-able libraries against the system `ldconfig -p` cache, which cannot be edited without root — therefore this sandbox's launch env sets `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1` and `__EGL_VENDOR_LIBRARY_DIRS` (both unnecessary on standard CI runners with real system packages; Chromium and Firefox launch here with no special env at all).
- **Full battery re-verified after wiring:** `npm run test` 1/1 green · `npm run lint` 0 errors/0 warnings (G-1) · `npm run typecheck` clean (G-2) · `npm run build` compiled successfully, 3/3 static pages (G-1/G-9 baseline) · `ci:lint-rules` PASS · `ci:no-placeholder-gate` PASS 0 violations (G-7; note: `.gitignore` extended with Playwright artifact dirs; build artifacts removed before commit — working tree clean).
- Commit: `feat(test): install Vitest/RTL/Playwright/axe toolchain + G-8 lint rules (M1-T002)` (this repository, 2026-09-05).

## Status
**COMPLETE**
