# M1-T002 — Install test/quality toolchain (Vitest, Testing Library, Playwright, axe, custom lint rules)

## Objective
Wire Vitest + RTL + Playwright + axe-core; add custom ESLint rules: no raw hex outside tokens, no dangerouslySetInnerHTML, no `any` (G-8).

## Why This Task Exists
Gates G-1/G-3/G-8 and every task's Tests section require the harness to exist first.

## Authoritative References
- AGENT.md §6
- TESTING.md §2
- CI-CD.md §4 G-1/G-3/G-8

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T017
- all feature tasks

## Scope
Test config + CI-ready scripts only; one smoke unit test proving the harness runs.

## Required Implementation
Configure browsers matrix per TESTING §3; add lint-rule implementations; zero fabricated test cases.

## Acceptance Criteria
- [ ] `npm run test` runs the smoke test green
- [ ] `npm run lint` enforces the three custom rules (verified with deliberate violations)
- [ ] Playwright launches Chromium/Firefox/WebKit

## Tests
Smoke unit test; lint violation fixtures.

## Verification
Toolchain outputs in evidence.

## Files / Areas Expected To Change
vitest/playwright configs, eslint rules, package.json

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**
