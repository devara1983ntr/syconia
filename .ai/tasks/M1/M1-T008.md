# M1-T008 — Primitives batch 1 — form controls (Button, IconButton, Input, Select, Textarea, Checkbox, Radio, Switch)

## Objective
Implement batch 1 with all states (default/hover/focus/active/pressed/disabled/loading) + Storybook entries.

## Why This Task Exists
DESIGN-SYSTEM §10 inventory is the component contract; batches keep reviews meaningful (D-002).

## Authoritative References
- DESIGN-SYSTEM.md §10/§4/§5
- ACCESSIBILITY.md §2/§7

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T003
- M1-T004
- M1-T006

## Blocks
- M1-T012
- M1-T015
- M2-T016

## Scope
Six form primitives + stories only.

## Required Implementation
Tokens only; ≥44px targets; focus ring token; loading state with ostiole dot on buttons.

## Acceptance Criteria
- [x] All states implemented + Storybook entries
- [x] axe zero critical per primitive
- [x] Keyboard operable

## Tests
RTL tests × states; axe per story.

## Verification
Storybook + test suite green.

## Files / Areas Expected To Change
/components/ui, /components/ui/stories

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent. Repository integrity at start: clean tree, HEAD `50dc14a` == `origin/main` (M1-T005 complete + hash recorded). Battery at start: 145/145, all gates green (regression confirmed before starting).
- **Scope note (title/scope mismatch resolved):** the task title lists all eight primitives (Button, IconButton, Input, Select, Textarea, Checkbox, Radio, Switch) while the Scope row says "Six form primitives + stories only" — the eight named components are implemented (title + MASTER-CHECKLIST row are the authoritative enumeration; "six" counts the field/choice controls behind Button/IconButton).
- **Dependencies installed (spec-justified only):** `lucide-react@1.41.0` (exact — DESIGN-SYSTEM §7/ICON-SYSTEM mandate; the icon library IS the spec's icon source; §7 stroke 1.5, 20/24px applied) · Storybook 10.6.0 exact (`storybook`, `@storybook/react-vite`, `@storybook/addon-a11y` — DESIGN-SYSTEM §10 "Storybook entry [REQUIRED]" per primitive, TESTING §8; verification row requires "Storybook + test suite green" at this task — M1-T018 then builds the visual-regression harness + full gallery) · `@testing-library/user-event@14.6.1` (dev — keyboard journeys, TESTING §6/§10).
- **`components/ui/states.ts` — the D-006b register (design-tokens.json interaction_states mandate):** control heights sm 44 / md 48 / lg 56 (all ≥ the §7 44px floor), §5-only text sizes (sm=text-meta 13, md/lg=text-body 16), choice visuals (18px box, 14px mark, 8px dot), switch track 40×24 + 16px thumb inside the 44×44 target, ostiole loader constants (§9: 1.2s, scale 1.12, 8px dot). Pinned by tests/unit/ui-states.test.ts together with the token sources (`--press-scale: 0.98` added to tokens.css :root — a D-006b token; the `.sy-press` components-layer class in globals.css applies it with transform-only §9 semantics).
- **`components/ui/button.tsx` (client):** `Button` — variants primary gold (Obsidian text on Ostiole Gold 8.1:1; hover Champagne) / secondary ghost (hairline border, surface hover) / destructive (error tone); sizes sm/md/lg; **loading = the ostiole dot** (m.span through the M1-T006 LazyMotion system: MOTION.loader 1.2s + scale 1.12 + opacity pulse, repeat reverse; `useReducedMotion` → static dot — §9 no loops); `aria-busy` + inert branch (disabled + loading share the D-006b surface/tertiary treatment — exclusive class branches, no equal-specificity conflicts); press via `.sy-press` (--press-scale token); 44px floors via `min-height: var(--target-min)`. `IconButton` — icon-only, **aria-label REQUIRED (type-enforced in IconButtonProps)**, ghost/solid, same size/state matrix, loading swaps the icon for the dot (ICON-SYSTEM: "loading is never an icon").
- **`components/ui/field.tsx` (client — useId wiring):** `Input` / `Select` / `Textarea` with the shared anatomy: programmatic label (htmlFor↔id) + required marker (decorative gold asterisk; aria-required on the control) + **hint slot + error slot** (aria-describedby chain; error → `aria-invalid`, `role="alert"` message, error border token). Native semantics throughout (ACCESSIBILITY §3: semantic HTML first — real `<select>` with semantic `<option>`s, lucide ChevronDown affordance at §7 stroke/size, decorative aria-hidden). 44px control floors; radius-sm (§6 "inputs"); D-006b disabled treatment.
- **`components/ui/choice.tsx` (client):** `Checkbox` / `Radio` — native inputs (keyboard + native radio-group arrows by construction), sr-only input + styled sibling carrying the visual: Ostiole Gold checked fill + Obsidian mark (§4), **focus ring mirrored onto the visual via peer-focus-visible outline tokens** (§2 never outline:none), the whole ≥44px label row is the hit area (§7 — the S-00 drawer row pattern); D-006b disabled rows. Verified in-browser: checked box bg = exact gold rgb(197,160,89), mark opacity reveal, label stays Alabaster.
- **`components/ui/switch.tsx` (client):** `role="switch"` on a native `<button>` (§3; Space/Enter by construction), `aria-checked`, aria-labelledby name, optional hint via aria-describedby, controlled (`checked`/`onCheckedChange`) + uncontrolled (`defaultChecked`), 44×44 target with the 40×24 track centered, transform-only thumb travel (§9), D-006b disabled.
- **Storybook (`.storybook/main.ts` + `preview.ts`):** react-vite framework + addon-a11y; preview imports the REAL token layer (`app/styles/tokens.css` + `globals.css` — Tailwind v4 via the Vite PostCSS pipeline; fonts fall back per the designed M1-T004 chain); backgrounds options use token vars (no raw hex). **`components/ui/stories/*.stories.tsx` — 52 stories across all 8 primitives** covering every state (default/hover via play-fns where applicable/focus/active-via-press/disabled/loading + per-variant/size/error/hint/checked matrices + "All states" visual matrices + keyboard play functions: tab/Space/Enter/arrows with assertions via `storybook/test`). Icons in stories come from the mapped inventory only (search/x/rotate-cw — ICON-SYSTEM rows). **`npx storybook build` → green** (52 entries in the index; Vite build 1.86s).
- **Tests (51 new, 196 total):** `tests/unit/ui-states.test.ts` (8: D-006b register + token sources) · `tests/unit/ui-button.test.tsx` (13: semantics, variants, size floors, loading aria-busy+dot+inert, disabled treatment, click+Space+Enter activation, axe across the state matrix — rendered inside the real MotionProvider) · `tests/unit/ui-field.test.tsx` (16: label/hint/error/required/disabled wiring + typing + select options/chevron/aria + textarea rows + axe per matrix) · `tests/unit/ui-choice.test.tsx` (14: checkbox label-row hit area + Space toggle + disabled; radio group native exclusivity + ArrowDown navigation; switch role/aria-checked/click+Space/controlled-uncontrolled/disabled/hint; axe per matrix) — **every axe run asserts zero critical violations (acceptance row 2); every keyboard journey uses userEvent (acceptance row 3)**.
- **G-7 gate false-positive resolved via the designed allowlist:** the literal word "placeholder" collides with the native HTML `placeholder` attribute + Tailwind `placeholder:` variant (UI features, not marker vocabulary). `components/ui/field.tsx` + the two field stories allowlisted with justification comments (allowlist is the reviewed CI-CD §4 mechanism; no gate logic changed).
- **In-browser verification (built Storybook served + inspected):** body bg = exact Obsidian rgb(9,9,11); primary button bg = exact Ostiole Gold rgb(197,160,89); destructive = exact error rgb(240,138,132); disabled = exact surface rgb(18,18,20); switch button = 44×44; track = exact surface-elevated rgb(26,26,30); checkbox checked = gold box + mark revealed + label Alabaster. Screenshots /tmp/qa-t008-button.png, /tmp/qa-t008-switch.png. 0 console errors.
- **Verification (all green):** `npm run test` → **196/196** (16 files) · `npm run typecheck` clean (incl. test files — caught and fixed 3 casts via the build's typecheck) · `npm run lint` 0/0 · `npm run ci:lint-rules` PASS (G-8) · `npm run ci:no-placeholder-gate` PASS (G-7, reviewed allowlist) · `npm run build` PASS (3 routes static) · `npx storybook build` PASS (52 stories).
- Commit: `8d8475a` — `feat(ui): primitives batch 1 — form controls with full states + Storybook gallery (M1-T008)` (pushed to origin/main 2026-09-05; worktree clean after push).

## Status
**COMPLETE**
