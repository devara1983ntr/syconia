# M1-T011 — Middleware: age-gate enforcement + security headers + request-id

## Objective
Edge middleware: block content/API without signed sy_age_ok (HMAC payload per SECURITY §6); emit CSP (nonce strict-dynamic, frame-src from enabled manifests) + full SECURITY §4 header set; inject x-request-id.

## Why This Task Exists
FR-1 is a product law; headers are the security backbone.

## Authoritative References
- SECURITY.md §4/§6
- PRD.md FR-1
- ARCHITECTURE.md §5
- API.md §3 exemptions (health/ready/csp-report/admin-login)

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T007

## Blocks
- M1-T012
- M2-T010

## Scope
middleware.ts + header/cookie libs only.

## Required Implementation
Age-exempt list exactly per API §3; CSP built from enabled-source manifests; HSTS preload value.

## Acceptance Criteria
- [ ] Content 403 without cookie; forged cookie 403
- [ ] Headers present on every route (automated test)
- [ ] CSP report-only phase → enforcement flip documented

## Tests
Header contract test + age probes (T-70 class).

## Verification
M1-GATE rows 1.6/1.7 evidence.

## Files / Areas Expected To Change
/middleware.ts,/lib

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**
