# M1-T001 — Android Gradle scaffold (Kotlin, Compose, Hilt, multi-module)

**Migration disposition (v1.1.0):** REPLACE (was Next.js scaffold)

## Objective
Gradle multi-module Android project per ARCHITECTURE §3 v1.1.0: /android with app, core/{common,designsystem,model,network,security,ui}, data/{repository,local}, domain, feature/* skeletons; Kotlin+Compose+M3+Hilt+Navigation-Compose wired; versions pinned per D-016 and recorded.

## Why This Task Exists
Every Android task builds on the scaffold; module graph enforces Clean Architecture.

## Authoritative References
- ARCHITECTURE.md §0–§3 v1.1.0
- AGENT.md §3
- SOP.md §1

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); toolchain per M0-T003; AGENT §6.

## Dependencies
- M0-T003

## Blocks
- all M1

## Scope
Scaffold + module structure + build config only; no features.

## Required Implementation
Wrapper-provisioned Gradle; convention plugins enforcing dependency direction (domain no-Android, UI no-Retrofit/Room); debug/release variants; empty app shell with SyconiaTheme applied.

## Non-functional Notes
- Security: No secrets in build config; debuggable only in debug.
- Accessibility: n/a
- Performance: Build time budget baseline recorded.

## Acceptance Criteria
- [ ] `./gradlew assembleDebug` green
- [ ] Module dependency-direction checks pass (convention plugin test)
- [ ] No feature code yet — honest empty shell

## Tests
Build + module-graph test.

## Verification
G-1/G-2 baseline green.

## Files / Areas Expected To Change
/android/**

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**


---

## Web-platform predecessor — COMPLETE (superseded)

- **Predecessor title:** M1-T001 — Initialize Next.js + TypeScript strict scaffold
- **Platform / status:** Web (pre-Android baseline) · **COMPLETE** — executed 2026-09-05 by the web-track agent session, all acceptance criteria PASS with evidence
- **Evidence commits** (preserved, reachable via the 2026-09-07 reconciliation merge): `feat 277a491` · `evidence f206a39`
- **Objective (web):** Next.js 15+ App Router app with TypeScript 5 strict, ESLint, repo layout per ARCHITECTURE §3 (/app route groups, /lib, /components, /tests, /scripts).
- **Disposition:** Superseded by the Android v1.1.0 redefinition above (this file's active content); the completed web implementation is **retained in-tree** (`app/`, `components/`, `lib/`, `tests/`, `scripts/`, web configs) as the web-platform track slated for M5. Full completion evidence: `git show f206a39:.ai/tasks/M1/M1-T001.md`.
