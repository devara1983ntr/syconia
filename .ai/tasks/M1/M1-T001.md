# M1-T001 — Initialize Next.js + TypeScript strict scaffold

## Objective
Next.js 15+ App Router app with TypeScript 5 strict, ESLint, repo layout per ARCHITECTURE §3 (/app route groups, /lib, /components, /tests, /scripts).

## Why This Task Exists
Every M1+ task builds on the scaffold; ARCHITECTURE §3 defines the canonical tree.

## Authoritative References
- ARCHITECTURE.md §2/§3
- AGENT.md §3 stack
- SOP.md §1

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M0-T003

## Blocks
- M1-T002
- all M1

## Scope
Scaffold + config only: tsconfig (strict, noUncheckedIndexedAccess), eslint base, package.json scripts per AGENT §6, .env handling.

## Required Implementation
create-next-app or manual scaffold matching §3 exactly; no business logic; no sample content.

## Acceptance Criteria
- [ ] `npm run build` succeeds on empty scaffold
- [ ] tsconfig strict flags present
- [ ] Repo tree matches ARCHITECTURE §3
- [ ] No demo/default content beyond framework-required files (stripped)

## Tests
`npm run build`; `npm run typecheck`.

## Verification
G-1/G-2 baseline green on scaffold.

## Files / Areas Expected To Change
/app, /lib, /components, /tests, /scripts, package.json, tsconfig.json, eslint config

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent. **Owner approval to start M1 explicitly given** in the owner's session directive of 2026-09-05: *"complete all the tasks … Don't wait for my approval"* (recorded here because CURRENT-STATE tracked M1-T001 as awaiting owner approval per .ai protocol; the approval condition is now satisfied; no gate was bypassed — M0-GATE PASS, DA-GATE non-blocking, M0-T003 COMPLETE).
- **Repository integrity at start:** working tree clean, `git fsck --full` clean, HEAD `a925278` == `origin/main` (verified by `git ls-remote origin HEAD` → `a92527849ff…`), credential-free remote URL, git identity `Zcode Agent <zcode-agent@users.noreply.local>`.
- **Scaffold created (manual, create-next-app equivalent):** `package.json` + `package-lock.json` (lockfile generated via `npm install --package-lock-only`, then installed via `npm ci` — lockfile-only rule honored); `tsconfig.json`; `eslint.config.mjs`; `next.config.ts`; `app/layout.tsx`; `app/(public)/page.tsx`; `.gitignore` extended (`next-env.d.ts`, `*.tsbuildinfo` — existing entries untouched, existing `.env.example`/`/.skills/`/`.env*` policy reused verbatim).
- **Locked stack honored (AGENT §3, ARCHITECTURE §2):** resolved in lockfile → `next@16.3.4` (15+ ✓), `react@19.2.8`, `react-dom@19.2.8`, `typescript@5.9.3` (TS 5 line — not the 7.x line; stack says "TypeScript 5"), `eslint@9.39.5`, `eslint-config-next@16.3.4` (flat-config-native subpath exports `core-web-vitals` + `typescript`; the legacy `FlatCompat`/`@eslint/eslintrc` approach was removed after it failed against the flat-native package). No other dependencies installed — Tailwind is M1-T003, Vitest/Playwright M1-T002, Drizzle M2-T001, Zod M1-T007, motion M1-T006, TanStack M2-T018, lucide-react M1-T005 (dependency minimalism per task scope "scaffold + config only").
- **Repo tree per ARCHITECTURE §3 (directory skeleton + framework-required files):** `/app` with `(public)` group (home `page.tsx` present — stripped to an empty semantic `<main>`; no demo content), route subdirectories for `search`, `categories/[slug]`, `tags/[slug]`, `watch/[slug]`, `(legal)/{terms,privacy,dmca,2257,cookies,about,contact}`, `offline`; `admin/{login,videos,categories,tags,sources,mappings,takedowns,settings,audit}`; `api/`; `/components/{ui,layout,player,cards,states,admin}`; `/lib/{db,adapters,auth,rate-limit,cache,analytics,validation,seo}`; `/tests`; `/scripts/ci`; `/drizzle`. Empty directories carry `.gitkeep` (git tracking mechanism — zero content; screen route files, `middleware.ts`, `sitemap.ts`/`robots.ts`/`not-found`/`error` arrive with their owning tasks M1-T011/T014/T016, M5-T002 — tree is the §3 *target* directory structure, files land with their tasks; noted in TRACEABILITY).
- **package.json scripts — full canonical AGENT §6 set present:** working now: `dev`, `build`, `start`, `lint`, `typecheck`, plus a real `ci:no-placeholder-gate` (G-7 script implemented below). Commands whose toolchain is owned by later .ai tasks are wired to `scripts/ci/tooling-pending.mjs`, which names the owning task and exits 2 (fail-closed gated state per AGENT §2.1.10 — an absent toolchain can never be mistaken for a passing run; QUALITY.md anticipates this: "commands exist once M1-T001/T002 create the toolchain"): `test`/`test:integration`/`test:e2e` → M1-T002; `db:migrate`/`db:generate` → M2-T001; `hash-password` → M4-T001; `perf:lighthouse` → M5-T001.
- **G-7 no-placeholder gate implemented for real** (`scripts/ci/no-placeholder-gate.sh` + reviewed allowlist `scripts/ci/no-placeholder-gate-allowlist.txt`): case-insensitive word-boundary grep of the CI-CD §4 vocabulary (`TODO`, `FIXME`, `TBD`, `XXX`, `HACK`, `mock`, `dummy`, `fake`, `lorem`, `placeholder`, `coming soon`, `not implemented`) across production paths `app/ lib/ components/ drizzle/ scripts/`, allowlist for reviewed exceptions (gate's own two files exempted — detection machinery carries the vocabulary; justification comments in the allowlist). Exit 1 with violations / exit 0 with summary.
- **.env handling:** pre-existing tracked `.env.example` (SOP §1.3/ARCHITECTURE §12 vocabulary) reused unchanged; `.gitignore` already carried `.env`, `.env.*`, `!.env.example` — verified by `git status` (no `.env*` besides the tracked example appears). No `.env.local` created: nothing consumes env vars until M1-T007 (`/lib/env.ts`), per scope "no business logic".
- **Acceptance criteria — all four PASS:**
  1. `npm run build` → **succeeded** (Next 16.3.4, Turbopack): routes `○ /` + `○ /_not-found`, 3/3 static pages generated, zero warnings.
  2. tsconfig strict flags → `strict: true` AND `noUncheckedIndexedAccess: true` (verified post-build; `jsx` auto-adjusted to `react-jsx` by next build — expected automatic-runtime behavior, strictness untouched).
  3. Repo tree matches ARCHITECTURE §3 → directory skeleton complete (see above; enumerated in commit).
  4. No demo/default content → home stripped to empty semantic `<main>`; layout stripped (no fonts/M1-T004, no metadata beyond canonical `SITE_NAME` title); no create-next-app demo page/assets/README; verified additionally by `npm run ci:no-placeholder-gate` PASS (0 violations) and manual review.
- **Task Tests:** `npm run build` PASS · `npm run typecheck` PASS (0 errors). **G-1/G-2 baseline:** `npm run lint` → 0 errors, 0 warnings (eslint-config-next flat native); `npm run typecheck` → clean strict. **G-7:** PASS 0 violations.
- **Dev-boot smoke (extra, beyond criteria):** `npx next dev -p 3100` → `GET /` → HTTP 200; served document contains exactly `<html lang="en"><…><title>SYCONIA</title>…<main aria-label="SYCONIA home"></main>` and no other content (verified via curl; server stopped after the check; build artifacts removed before commit).
- Commit: `feat(app): initialize Next.js + TypeScript strict scaffold (M1-T001)` (this repository, 2026-09-05).

## Status
**COMPLETE**
