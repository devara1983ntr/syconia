# M1-T006 — Motion spec in Compose (DS §9 values)

**Migration disposition (v1.1.0):** MODIFY (was motion/react config)

## Objective
Shared Compose animation specs implementing DS §9 exactly (durations/easings table); reduced-motion detection (animation-scale settings) with static-emblem fallback.

## Why This Task Exists
Brand motion law: transform/opacity only; reduced-motion honored (ACCESSIBILITY §6 v1.1.0).

## Authoritative References
- DESIGN-SYSTEM.md §9
- ACCESSIBILITY.md §6

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); toolchain per M0-T003; AGENT §6.

## Dependencies
- M1-T003

## Blocks
- M1-T012+
- all animated UI

## Scope
Motion tokens + reusable spec objects only.

## Required Implementation
No infinite animations besides ostiole loader (pauses off-screen); predictive-back transitions enabled.

## Non-functional Notes
- Security: n/a
- Accessibility: Reduced-motion: ≤100ms opacity-only, no loops.
- Performance: Frame budget respected.

## Acceptance Criteria
- [ ] Spec objects match §9 table (unit)
- [ ] Reduced-motion renders static emblem (screenshot)

## Tests
Unit + screenshot tests.

## Verification
Suite green.

## Files / Areas Expected To Change
/android/core/designsystem

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**


---

## Web-platform predecessor — COMPLETE (superseded)

- **Predecessor title:** M1-T006 — Configure Motion (motion/react LazyMotion)
- **Platform / status:** Web (pre-Android baseline) · **COMPLETE** — executed 2026-09-05 by the web-track agent session, all acceptance criteria PASS with evidence
- **Evidence commits** (preserved, reachable via the 2026-09-07 reconciliation merge): `feat e273ab8`
- **Objective (web):** LazyMotion + domAnimation feature bundle, page/component transition variants per DS §9, reduced-motion utilities.
- **Disposition:** Superseded by the Android v1.1.0 redefinition above (this file's active content); the completed web implementation is **retained in-tree** (`app/`, `components/`, `lib/`, `tests/`, `scripts/`, web configs) as the web-platform track slated for M5. Full completion evidence: `git show e273ab8:.ai/tasks/M1/M1-T006.md`.
