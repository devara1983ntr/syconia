# M1-T006 — Configure Motion (motion/react LazyMotion)

## Objective
LazyMotion + domAnimation feature bundle, page/component transition variants per DS §9, reduced-motion utilities.

## Why This Task Exists
Motion spec requires transform/opacity-only, reduced-motion honored; LazyMotion keeps JS budget.

## Authoritative References
- DESIGN-SYSTEM.md §9
- AGENT.md §3
- PERFORMANCE.md §3

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T013
- all animated UI

## Scope
Motion provider + shared variants (drawer, modal, card, toast, stagger) only.

## Required Implementation
Variants encode §9 durations/easings; reduced-motion variant switch.

## Acceptance Criteria
- [x] Transitions match §9 table
- [x] prefers-reduced-motion renders ≤100ms opacity-only
- [x] No layout-property animation

## Tests
Component motion tests incl. reduced-motion.

## Verification
G-7/G-9 green.

## Files / Areas Expected To Change
/lib/motion, providers

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent. Repository integrity at start: clean tree, HEAD `dcd3ec4` == `origin/main`.
- **Dependency (locked stack, ARCHITECTURE §2):** `motion@13.2.0` installed `--save-exact` (runtime dependency; registry-verified in M0-T003; lockfile updated, `npm install` clean, 0 vulnerabilities).
- **`lib/motion/provider.tsx` ("use client"):** `LazyMotion features={domAnimation} strict` — the PERFORMANCE §3-mandated pattern (no full domMax on public routes). `strict` forbids `motion.*` — all animated components must use the lightweight `m.*` (runtime-enforced; test-proven: rendering `motion.div` inside the provider throws). Mounted exactly once at the app root (`app/layout.tsx` wraps `{children}`), never per-route. domAnimation covers §9 needs (variants, exit via AnimatePresence, hover/tap/focus gestures); drag/layoutId need domMax and arrive with their owning tasks (watch shared-element, M3) to be budget-re-evaluated there.
- **`lib/motion/variants.ts`:** `MOTION` timing tokens verbatim from the §9 table (page 180ms ease-out + translateY 8 · drawer 260ms cubic-bezier(0.22,1,0.36,1) · scrim 200ms · modal 200ms scale .96→1 · sheet 280ms translateY 100%→0 · card 180ms scale 1→1.02 + veil 0.4→0 · toast 200ms slide-up+fade (4s auto-dismiss constant; distance `TOAST_TRAVEL=16` documented as the one §9-unspecified parameter, tunable at M1-T009) · stagger 40ms/cap 12 · shimmer 1.6s + loader 1.2s scale 1.12 constants as the single timing source for their owning primitives). Variant families: page, scrim, drawer, modal, sheet, card, cardVeil, toast, stagger, staggerChild. `selectVariants(reduced)` = the reduced-motion switch; `capStaggerChildren`/`STAGGER_CAP` enforce the §9/P §4 “≤12 staggered nodes” law.
- **Reduced-motion set (`reducedVariants`):** every transition = opacity-only, duration exactly 100ms (`MOTION.reducedMax`); zero transform keys anywhere (drawers/sheets/modals fade in/out instead of sliding; card hover/focus targets are empty — no hover transforms; veil holds static rest opacity; staggerChildren 0; no repeat/loop). Family parity with the standard set.
- **Tests (26 total, all green):** `tests/unit/motion-variants.test.ts` (22) — §9 table verbatim asserts (durations, drawer bezier array, per-row initial/animate targets, toast constants, stagger 40ms/12) · PERFORMANCE §4 law walker (no animated key outside {x, y, scale, opacity} in ANY standard state) · reduced-motion laws (every duration ≤ 0.1s, every key opacity-only, no loops, family parity, staggerChildren 0) · `selectVariants` identity · cap helper (20→12, 12→12, 0→0). `tests/component/motion-provider.test.tsx` (4) — provider renders children and runs the page animation to opacity 1 in jsdom · §9 initial state applied synchronously (opacity "0", transform "translateY(8px)") · strict mode rejects `motion.*` (toThrow) · reduced-vs-standard observable difference (standard sets translateY transform, reduced never sets any transform; both reach opacity 1).
- **G-9 bundle evidence (honest before/after, gzip first-load JS of the prerendered home HTML):** WITHOUT provider 168.3KB (6 chunks — the immutable Next.js 16 + React 19.2 App-Router framework floor, present before this task) → WITH provider 190.7KB (7 chunks) — **motion adds exactly +22.4KB gzip** (the LazyMotion domAnimation subset; the full import would be materially larger). App-controlled JS = 22.4KB. Note recorded: the §3 “≤120KB (home)” figure cannot be an absolute page total for ANY Next 16 App Router page (framework floor alone is ~168KB) — the G-9 gate script (arriving with its owning task per the tooling-pending registry, M5-T001) must define the budget as app-controlled JS or total-minus-framework; numbers above are the baseline data for that decision.
- **Verification (all green):** `npm run test` → **60/60** (5 files: home smoke, tokens 17, fonts 14+3, motion-variants 22, motion-provider 4) · `npm run typecheck` clean · `npm run lint` 0/0 · `npm run ci:no-placeholder-gate` PASS (G-7) · `npm run ci:lint-rules` PASS (G-8) · `npm run build` PASS (3/3 static) · prod-boot smoke `next start :3100` → GET / 200 with the home landmark intact (provider SSR-safe).
- Commit: `feat(motion): wire LazyMotion domAnimation provider + §9 variant system (M1-T006)` (this repository, 2026-09-05).

## Status
**COMPLETE**
