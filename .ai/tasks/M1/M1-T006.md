# M1-T006 — Configure Motion (motion/react LazyMotion)

## Objective
LazyMotion + domAnimation feature bundle, page/component transition variants per DS §9, reduced-motion utilities.

## Why This Task Exists
Motion spec requires transform/opacity-only, reduced-motion honored; LazyMotion keeps JS budget.

## Authoritative References
- DESIGN-SYSTEM.md §9
- AGENT.md §3
- PERFORMANCE.md §3

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- M1-T001

## Blocks
- M1-T013
- all animated UI

## Scope
Motion provider + shared variants (drawer, modal, card, toast, stagger) only.

## Required Implementation
Variants encode §9 durations/easings; reduced-motion variant switch.

## Acceptance Criteria
- [ ] Transitions match §9 table
- [ ] prefers-reduced-motion renders ≤100ms opacity-only
- [ ] No layout-property animation

## Tests
Component motion tests incl. reduced-motion.

## Verification
G-7/G-9 green.

## Files / Areas Expected To Change
/lib/motion, providers

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**
