# M1-T019 — Data/domain architecture layer (Retrofit services, DTO mapping, repositories, DataStore)

**Migration disposition (v1.1.0):** REPLACE (supersedes retired M2-T018 TanStack layer)

## Objective
core/network Retrofit services against API.md §4 contracts; DTO→domain mappers (data layer only); repository implementations with TTL caching (PRD2 §5), retry/backoff, request supersession (Flow); DataStore session/age/settings stores (sy_sid lifecycle).

## Why This Task Exists
The client data plane replacing TanStack Query (migration classification); all features consume repositories.

## Authoritative References
- API.md (contracts)
- PRD2.md §2.7/§5
- ARCHITECTURE §2 v1.1.0
- D-011/D-014

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); toolchain per M0-T003; AGENT §6.

## Dependencies
- M1-T001
- M1-T007

## Blocks
- M5-T010…T013
- M3-T004
- M3-T006

## Scope
Data/domain skeleton against contracts with recorded fixtures (no live backend yet — honest errors until M2).

## Required Implementation
DTOs never leak past data layer; domain pure Kotlin; fixture-based contract tests (recorded shapes only); session id lifecycle per PRD2.

## Non-functional Notes
- Security: TLS-only; no secrets; URL validation on every request.
- Accessibility: n/a
- Performance: Retry budgets; no main-thread IO.

## Acceptance Criteria
- [ ] Contract tests vs recorded API fixtures green
- [ ] Supersession test: stale results discarded
- [ ] DTO isolation enforced (lint test)

## Tests
Unit + repository tests.

## Verification
Suite green.

## Files / Areas Expected To Change
/android/core/network,/android/data,/android/domain

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**
