# M0-T003 — Verify Android + backend toolchain prerequisites

**Migration disposition (v1.1.0):** MODIFY (was: web toolchain; redefined for Android v1.1.0)

## Objective
Confirm the dual toolchain before any implementation: JDK 17+, Android SDK (current platform + build-tools) via Android Studio or cmdline-tools, Gradle wrapper build succeeds on a skeleton, emulator bootable (API 26…current); backend: Node 20+, npm 10+, PostgreSQL 16 reachable (local or Neon branch).

## Why This Task Exists
All M1 tasks assume both toolchains; failing fast prevents half-configured repos.

## Authoritative References
- AGENT.md §6 (v1.1.0 commands)
- SOP.md §1 (v1.1.0)
- ARCHITECTURE.md §2

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); toolchain per M0-T003; AGENT §6.

## Dependencies
- M0-T004

## Blocks
- M1-T001
- M1-GATE

## Scope
Environment verification only — no project code (scaffold is M1-T001).

## Required Implementation
Run version checks on both toolchains; record outputs. If unavailable, record BLOCKER — never fake.

## Non-functional Notes
- Security: No secrets in recorded outputs.
- Accessibility: n/a
- Performance: n/a

## Acceptance Criteria
- [ ] java -version ≥17; ANDROID_HOME set; sdkmanager lists platform; avd boot OK; node ≥20; npm ≥10; psql reachable

## Tests
Version command outputs + emulator boot log.

## Verification
Outputs pasted into Completion Evidence.

## Files / Areas Expected To Change
none (environment only)

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
Executed 2026-09-07 (this session; all commands actually run — outputs below are the real recorded results):

**ANDROID — build path VERIFIED:**
- JDK: Temurin **17.0.20.1** (`/opt/jdk17/bin/java -version`) — ≥17 PASS (system default Java is 11; JDK 17 installed explicitly; apt had no openjdk-17 — Temurin tarball used).
- Android SDK: cmdline-tools 11076708 installed at `/opt/android-sdk`; `sdkmanager --list_installed` → **build-tools;35.0.0 · platform-tools 37.0.1 (adb 1.0.41) · platforms;android-35** — PASS ("sdkmanager lists platform").
- Gradle: **8.10.2** at /opt/gradle; `gradle --version` → Gradle 8.10.2, embedded Kotlin **1.9.24**, Launcher JVM 17.0.20.1 — PASS.
- Wrapper diagnostic (tiny artifact, /tmp, not the repo): `gradle wrapper --gradle-version 8.10.2` + `./gradlew --version` → **PASS** (note: wrapper task requires a settings file to exist first — recorded for M1-T001). Skeleton deleted after verification.
- Compose/AGP toolchain: resolution paths verified reachable (maven.google.com 200, repo.maven.apache.org 200); compatibility matrix satisfied: Gradle 8.10.2 ↔ AGP 8.7–8.9 (requires Gradle ≥8.9 + JDK 17) ↔ Kotlin 1.9.24/2.x. Exact pins happen at M1-T001 per **D-016** (no versions invented now).
- ANDROID — emulator/instrumented path **BLOCKED (B-004)**: `/dev/kvm` ABSENT, 2 vCPU / 2 GB RAM → AVD boot structurally impossible in this sandbox (AC "avd boot OK" cannot pass here; no fake claim made).

**BACKEND — VERIFIED:** Node **20.20.2** · npm **10.8.2** · git **2.47.3** · **PostgreSQL 17.11 server installed, started, and accepting connections** (`pg_isready -h localhost:5432` → accepting; `SELECT version()` → 17.11; ≥16 PASS) · npm registry reachable (200).

**TESTING:** Gradle test execution capability proven (Gradle runs + wrapper green); instrumented/emulator tests blocked by B-004 in this sandbox; backend browser tooling (Playwright) intentionally not installed yet (M1-T002 backend track; registry reachable).

**SKILLS:** `.skills/ui-ux-pro-max` PRESENT (v2.13.0 clone; advisory-only, git-ignored). **"Motion" skill: ABSENT** — never cloned into this workspace; motion authority remains DESIGN-SYSTEM.md §9 (spec) — recorded honestly, not fabricated.

**Environment persistence caveat (honest):** /opt + system installs (JDK/SDK/Gradle/PG server) are session-scoped in this sandbox; the recorded commands re-provision any session in ~2 minutes. Snapshot-safe (nothing written into /home/user caches).

**Dependency analysis (per directive §13):** build-path tasks M1-T001…M1-T016, M1-T018, M1-T019 and backend M2/M4 tasks are NOT blocked (no emulator needed to build/unit-test); blocked-in-sandbox: M1-T017 (connected E2E) and every `connected*AndroidTest`/instrumented row until B-004 is resolved (operator hardware or KVM-capable CI).

## Status
**BLOCKED**

> **BLOCKED by B-004** (emulator/AVD capability impossible in this execution sandbox: no /dev/kvm, 2 vCPU/2 GB). Everything verifiable locally is VERIFIED above with real outputs. Resolution options recorded in BLOCKERS.md B-004. Build-path M1 work is explicitly NOT blocked per the dependency analysis above.
