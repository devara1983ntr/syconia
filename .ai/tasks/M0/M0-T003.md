# M0-T003 — Verify M1 toolchain prerequisites

## Objective
Confirm Node 20+, npm 10+, PostgreSQL 16 reachable (local or Neon branch), Playwright browsers installable, before scaffolding.

## Why This Task Exists
AGENT §6 commands and all M1 tasks assume the toolchain; failing fast prevents half-configured repos.

## Authoritative References
- AGENT.md §6 commands
- SOP.md §1 setup
- ARCHITECTURE.md §2 stack

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); development environment per AGENT §6.

## Dependencies
- -
-  
- M
- 0
- -
- T
- 0
- 0
- 2

## Blocks
- -
-  
- M
- 1
- -
- T
- 0
- 0
- 1
- 

- -
-  
- M
- 1
- -
- G
- A
- T
- E

## Scope
Local environment verification only — no project code.

## Required Implementation
Run version checks; record outputs. If unavailable, record BLOCKER — do not fake.

## Acceptance Criteria
- [x] node -v ≥20; npm -v ≥10; psql/pg reachable; npx playwright --version works

## Tests
Version command outputs.

## Verification
Outputs pasted into Completion Evidence.

## Files / Areas Expected To Change
none (environment only)

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values; no dead code. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- Executed 2026-09-05 by ZCode agent (session trace `1a06fc1db263bca5`), environment: Debian 13 (trixie) x86_64, non-root user `z`.
- **node** → `v24.19.0` (≥20 PASS).
- **npm** → `11.17.0` (≥10 PASS).
- **psql / PostgreSQL 16+ reachable** → PASS. No system PostgreSQL existed and no root/sudo is available in this environment (sudo requires an unavailable password), so the documented prerequisite was provisioned safely in-environment: official Debian packages `postgresql-17` + `postgresql-client-17` (`17.11-0+deb13u1`, apt SHA256-verified) downloaded and extracted rootless via `dpkg -x` to `/home/z/.local/share/postgres-rootless/`; server initialized (`initdb`, encoding UTF8, locale C) and running at `127.0.0.1:5432`. Evidence: `pg_isready -h 127.0.0.1 -p 5432` → `accepting connections`; `psql … -c "SELECT version();"` → `PostgreSQL 17.11 (Debian 17.11-0+deb13u1) on x86_64-pc-linux-gnu…`; `server_version_num` = `170011`; empty database `syconia` created (no records — zero-placeholder law). Control script: `/home/z/.local/share/syconia-pg/pg.sh` (start|stop|status|restart). Production database remains Neon per DEPLOYMENT.md §1 (operator supplies `DATABASE_URL`); pg_trgm/pgcrypto modules available for M2.
- **npx playwright --version** → `Version 1.62.1` (PASS). Browsers: `chromium-1234`, `chromium_headless_shell-1234`, `ffmpeg-1011` present in `~/.cache/ms-playwright` (install dry-run confirms locations) — browsers installable and installed.
- **Additional prerequisites (owner directive)**: `git version 2.47.3` ✓; `Python 3.12.14` ✓ (required by the UI/UX Pro Max skill scripts per SOP.md §5 — verified working); git identity configured (repo-local agent identity for commits; global `Z User <z@container>`, `init.defaultBranch=main`); toolchain/registry — locked-stack resolution verified via `npm view`: `next 16.3.4` (15+ ✓), `typescript 7.0.2` (5.x line also resolvable), `drizzle-orm 0.45.2`, `drizzle-kit 0.31.10`, `motion 13.2.0`, `@tanstack/react-query 5.102.8` (v5 ✓), `lucide-react 1.41.0`, `tailwindcss 4.3.3` (v4 ✓), `zod 4.5.4`, `vitest 5.0.0`, `@playwright/test 1.63.0`, `axe-core 4.13.0`.
- **Repository integrity** — working tree clean (`git status --porcelain` empty), `git fsck --full` clean, HEAD `6d3cdd3` == `origin/main` at execution time, remote URL credential-free.
- **ZCode skill prerequisites (SOP.md §5)** — official `ui-ux-pro-max` skill v2.13.0 (upstream `nextlevelbuilder/ui-ux-pro-max-skill` @ `f3ac195`, MIT, Python-stdlib-only scripts, no network/install-time execution) installed at git-ignored `/.skills/ui-ux-pro-max/` per ARCHITECTURE.md §3 — payload byte-identical to upstream, BM25 search verified working. An adapted Motion skill (`motion/react` import contract, reduced-motion, LazyMotion per M1-T006) is installed in the agent-side skill registry only — not in this repository.
- **No application dependencies installed (by design, not omission)** — the repository intentionally contains no `package.json`; scaffold + dependency installation is M1-T001 scope (this task's `Blocks`). `motion`, `@tanstack/react-query`, `lucide-react` verified resolvable from the npm registry instead; nothing to duplicate (no manifest exists).
- Commit: `chore(ai): record M0-T003 toolchain verification (M0-T003)` (this repository, 2026-09-05).

## Status
**COMPLETE**
