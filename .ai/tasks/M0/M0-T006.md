# M0-T006 — Isolate historical web implementation under /web (history-preserving move)

**Created:** 2026-09-07 (owner directive: actual move DEFERRED out of the documentation phase).

## Objective
Physically isolate the historical/superseded web client implementation into `/web` using history-preserving `git mv`, after a documented inventory classifies every root-level web-era artifact as web-client vs backend-surface.

## Why This Task Exists
The web client is superseded as primary platform but must be preserved (audit + M5 retained-surface decisions); a clean three-platform tree (web/ · android/ · backend/) requires the physical separation, done deliberately — not during documentation or Android implementation.

## Authoritative References
- ARCHITECTURE.md §0/§3 (boundaries, D-023)
- M0-T005 (identity/boundary documentation this task executes against)

## Prerequisites
- M0-T005

## Dependencies
- M0-T005

## Blocks
- M5 web-track tasks (retained-surface decisions consume the isolated tree)

## Scope
Inventory + classification (components/, web-client configs/tests vs backend app/api+admin+legal, lib/, drizzle/, scripts/) → `git mv` web-client artifacts to /web → adjust package.json/tsconfig/eslint/vitest/playwright/storybook paths for BOTH trees → validate: `npm ci`, `npm run build`, `npm run test`, `npm run lint` green → single commit → verify tree + remote. **No Android work inside this task. No history rewriting.**

## Required Implementation
Explicit move pipeline: current location → target /web → validation → build/test → commit → verification (owner directive §3, 2026-09-07).

## Non-functional Notes
- Git history preservation: moves via `git mv` (rename tracking), never delete-then-recreate.

## Acceptance Criteria
- [ ] Inventory document classifies every root web-era artifact (web-client vs backend) — no orphans
- [ ] Web client lives under /web with git rename tracking; backend untouched in function
- [ ] Backend + web builds/tests green post-move (npm ci/build/test/lint)
- [ ] Single clean commit; remote verified

## Tests
npm build/test/lint on both planes post-move.

## Verification
Tree comparison pre/post; git log --follow sanity on moved files.

## Files / Areas Expected To Change
/web/** (new), root web-client configs, package.json scripts, CI-CD.md paths (if affected)

## Forbidden Shortcuts
No silent discards; no rewriting history; no fake classification (every file accounted for).

## Completion Evidence
- (fill on execution)

## Status
**NOT_STARTED**
