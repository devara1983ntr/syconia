# M0-T006 — Isolate historical web implementation under /web (history-preserving move)

**Created:** 2026-09-07 (owner directive: actual move DEFERRED out of the documentation phase).

## Objective
Physically isolate the historical/superseded web client implementation into `/web` using history-preserving `git mv`, after a documented inventory classifies every root-level web-era artifact as web-client vs backend-surface.

## Why This Task Exists
The web client is superseded as primary platform but must be preserved (audit + M5 retained-surface decisions); a clean three-platform tree (web/ · android/ · backend/) requires the physical separation, done deliberately — not during documentation or Android implementation.

## Authoritative References
- ARCHITECTURE.md §0/§3 (boundaries, D-023)
- M0-T005 (identity/boundary documentation this task executes against)

## Prerequisites
- M0-T005

## Dependencies
- M0-T005

## Blocks
- M5 web-track tasks (retained-surface decisions consume the isolated tree)

## Scope
Inventory + classification (components/, web-client configs/tests vs backend app/api+admin+legal, lib/, drizzle/, scripts/) → `git mv` web-client artifacts to /web → adjust package.json/tsconfig/eslint/vitest/playwright/storybook paths for BOTH trees → validate: `npm ci`, `npm run build`, `npm run test`, `npm run lint` green → single commit → verify tree + remote. **No Android work inside this task. No history rewriting.**

## Required Implementation
Explicit move pipeline: current location → target /web → validation → build/test → commit → verification (owner directive §3, 2026-09-07).

## Non-functional Notes
- Git history preservation: moves via `git mv` (rename tracking), never delete-then-recreate.

## Acceptance Criteria
- [ ] Inventory document classifies every root web-era artifact (web-client vs backend) — no orphans
- [ ] Web client lives under /web with git rename tracking; backend untouched in function
- [ ] Backend + web builds/tests green post-move (npm ci/build/test/lint)
- [ ] Single clean commit; remote verified

## Tests
npm build/test/lint on both planes post-move.

## Verification
Tree comparison pre/post; git log --follow sanity on moved files.

## Files / Areas Expected To Change
/web/** (new), root web-client configs, package.json scripts, CI-CD.md paths (if affected)

## Forbidden Shortcuts
No silent discards; no rewriting history; no fake classification (every file accounted for).

## Completion Evidence
- Executed 2026-09-07 (owner directive: physical web isolation only; Android untouched).
- **Inventory & classification (full table in TRACEABILITY):** WEB → .storybook/ (2), components/ui/stories (19), tests/unit/ui-* (13) · BACKEND → app/, lib/, drizzle/, scripts/, instrumentation.ts, next.config.ts · SHARED → components/** (minus stories), public/branding (manifest/favicon/OG for retained web surfaces) · TOOLING → package.json, tsconfig, eslint/vitest/playwright/postcss configs, tests/lint-fixtures, tests/vitest-setup · DOCUMENTATION → canonical .md suite, docs/, .ai/ · GENERATED → storybook-static/, .next/ (ignored, removed pre-commit). Nothing classified DO-NOT-MOVE beyond the above retentions.
- **Moved (git mv, rename-tracked):** `.storybook/` → `web/.storybook/`; `components/ui/stories/` → `web/stories/ui/` (19 stories); `tests/unit/ui-*.test.*` → `web/tests/` (13 tests; `@/`-alias imports — unchanged).
- **Intentionally retained at root (backend/shared, per D-023 matrix):** app/ (api/admin/legal route skeletons + layout/fonts/manifest/styles serve the backend's retained web surfaces), lib/ (env, validation, motion provider used by root layout), components/** library (future admin-console UI kit), public/** (backend-served assets), all backend configs. **Backend exclusions honored:** nothing from app/, lib/, drizzle/, scripts/ moved.
- **Path/import reconciliation:** 19 story relative imports → `../../../components/ui/*` (initial two-level rewrite caught by typecheck and fixed — 54 errors → 0); `web/.storybook/main.ts` stories glob + staticDirs `../../public`; `web/.storybook/preview.tsx` provider/token imports; `vitest.config.ts` include += `web/tests/ui-*.test.{ts,tsx}`; `package.json` storybook/build-storybook `-c web/.storybook`; allowlist story paths.
- **Verification (real runs, Node 22.23.2 after environment fix):** `npm run lint` 0/0 · `npm run typecheck` clean · `npx vitest run` **25 files, 276 passed / 3 skipped** (both planes; 13 moved files green) · `npm run build` green (static routes + manifest.webmanifest) · `npm run build-storybook` green. **Node note:** vitest 5.0.0 forks pool fails on sandbox Node 20.20.2 (`webidl.util.markAsUncloneable`) — Node 22.23.2 provisioned at /opt/node22, zero repo changes. **Playwright e2e:** not executed this session (browser binaries not provisioned); spec `tests/e2e/brand-visual.spec.ts` + snapshots unmoved and unaffected by the move.
- **Backend verification:** same `next build` compile of all backend surfaces green; lib/env + lib/validation + instrumentation tests green (within the 276); drizzle config untouched; no Android dependencies anywhere (zero Android artifacts verified).

## Status
**COMPLETE**
