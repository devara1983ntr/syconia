# M3-T001 — Player shell: stage, poster→init, watermark, capability chrome (Compose + WebView)

**Migration disposition (v1.1.0):** MODIFY (player shell now Android)

## Objective
Watch stage (16:9 reserved), poster + ostiole affordance, WebView embed initialization per capability set, official-asset watermark overlay (branding pack spec), chrome rendered only per capability.

## Why This Task Exists
Core watch experience; WebView is the honest playback mechanism for official embeds (D-013).

## Authoritative References
- SCREENS.md S-07
- GESTURES.md §2/§6
- branding/README §Watermark
- D-013

## Prerequisites
Phase gate of the previous phase PASS (see ROADMAP); toolchain per M0-T003; AGENT §6.

## Dependencies
- M2-GATE (phase gate)
- M1-T010
- M2-T009

## Blocks
- M3-T003
- M3-T004

## Scope
Shell + WebView container + chrome only.

## Required Implementation
Hardened WebView per SECURITY §2A (allowlisted URLs only, JS for embed function, no file access, safe browsing); watermark 128px@20% bottom-right in our layer — never inside the embed.

## Non-functional Notes
- Security: WebView policy test (allowlist enforcement).
- Accessibility: Stage labeled region; watermark hidden from a11y.
- Performance: Embed init ≤600ms after intent.

## Acceptance Criteria
- [ ] Capability-driven chrome proven (param tests)
- [ ] No non-allowlisted URL ever loads (policy test)

## Tests
Instrumented WebView tests.

## Verification
M3-GATE 3.1 precursor.

## Files / Areas Expected To Change
/android/feature/watch,core/security

## Forbidden Shortcuts
No placeholders/mock/dummy data or logic; no TODO/FIXME markers; no partial implementation declared complete; no fake return values. AGENT.md §2 (laws) + §2.1 zero-placeholder policy; never weaken a test to pass it; no invented scope.

## Completion Evidence
- (fill on completion: commands, outputs, commit SHA)

## Status
**NOT_STARTED**
